# מוד מסך הבית — Qin 1s+ (ClassicHome Mod)

מוד ל-launcher המקורי של Qin 1s+ (Duoqin, Android 4.4.4, API 19, מסך 240×320 ldpi, מקלדת פיזית בלבד, עברית/RTL).
חבילת המוד: `com.sprd.simple.launcher.mod`. המקורית (`com.sprd.simple.launcher`) נשארת במכשיר.

## שלושת כללי הברזל של הפרויקט

1. **רק הקבצים שב-`TARGET_CLASSES` (בתוך `build_and_deploy.py`) מתקמפלים מ-Java.**
   כרגע 10 קבצים. כל שאר `src/` הוא פלט jadx לתיעוד בלבד — עריכתו לא משפיעה על ה-APK.
   קלאס חדש חייב להתווסף לרשימה, אחרת `NoClassDefFoundError` שקט במכשיר.
2. **משאב חדש שנקרא מ-Java:** הדפוס הקיים הוא `getResources().getIdentifier(...)` בזמן ריצה
   (בלי צנרת). גישה דרך קבוע `R.*` דורשת רישום כפול — `public.xml` + `R.java` (פירוט בסקיל
   הבנייה). מחרוזות UI בעברית נכתבות כליטרלים בקוד — זה הדפוס הקיים.
3. **`python build_and_deploy.py` רק בונה. התקנה: `python build_and_deploy.py --deploy`.**
   העברת קלאס smali קיים ל-Java מחייבת גם את שער ה-parity הידני — ראה הסקיל `qin-launcher-build`.

## סקילים (`.claude/skills/` — נטענים לפי צורך)

| סקיל | מתי |
| :--- | :--- |
| `qin-launcher-build` | כל בנייה/פריסה/הוספת קלאס או משאב, ותקלות בנייה |
| `qin-device` | עבודה מול המכשיר: adb, צילומי מסך, ניווט מקשים, אימות פיקסלים |
| `qin-newui` | כל UI חדש או תיקון עיצוב — ערכת NewUI, שורות, דיאלוגים, feature bar |
| `qin-launcher-map` | מפת הקוד: מה רץ מאיפה, מפתחות ההגדרות, מנוע הקיצורים |
| `qin-mouse-context` | רקע על העכבר המובנה ופרויקט StarMouse (מצב עריכת ווידג'טים ועוד) |

## חוזה תיעוד — לשמור על המסמכים חיים

בסיום כל משימה שמשנה אחד מאלה, לעדכן באותו קומיט:

| מה השתנה | מה מעדכנים |
| :--- | :--- |
| צנרת הבנייה / `TARGET_CLASSES` | הסקיל `qin-launcher-build` + הכללים כאן |
| התנהגות מקשים, הגדרה חדשה, מסך חדש | הסקיל `qin-launcher-map` |
| ידע עיצוב חדש (משאב framework, מלכודת UI) | הסקיל `qin-newui` |
| דרך עבודה חדשה מול המכשיר / מלכודת adb | הסקיל `qin-device` |
| פיצ'ר הושלם / בוטל | `UPGRADE_PLAN.md` (לסמן סטטוס), `PROJECT_MAP.md` אם השתנה המבנה |
| הוראות שימוש/התקנה למשתמש | `README.md` (קהל היעד: משתמש קצה; פרטים טכניים — הפניה ל-PROJECT_MAP) |

כללים: לתעד **עובדות שנבדקו** בלבד (לציין תאריך אימות בדברים רגישים); מלכודת חדשה נכנסת
לרשימת המלכודות של הסקיל הרלוונטי; אין לשכפל תוכן בין מסמכים — קישור במקום העתקה;
מחקר חד-פעמי גדול נשמר ב-`plan_research/` והסקיל מפנה אליו.

## פרויקט אח — העכבר

`C:\Users\a0527\Desktop\עכבר במכשיר מקשים` — אפליקציית StarMouse (עכבר בלחיצה ארוכה על `*`),
עם `GUIDE.md` מקיף שכולל 30 מלכודות מאומתות על אותו מכשיר. לפני מלחמה בבעיה מוזרה במכשיר —
לבדוק אם היא כבר מתועדת שם. תקציר רלוונטי ללאנצ'ר: בסקיל `qin-mouse-context`.

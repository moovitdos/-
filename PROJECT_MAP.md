# מפת הפרויקט — מוד מסך הבית Qin 1s+

עודכן: 2026-08-08. מסמך זה הוא מפת-העל למי שפותח את הפרויקט; הידע התפעולי המפורט יושב
בסקילים שב-`.claude/skills/` (נטענים לפי צורך ב-Claude Code) וב-`CLAUDE.md` (כללי הברזל).

## מה זה

מוד ל-launcher המקורי (ClassicHome) של Qin 1s+ ‏(Duoqin, Android 4.4.4, ‏API 19, ‏240×320,
מקלדת בלבד, עברית/RTL). מותקן לצד המקורי כ-`com.sprd.simple.launcher.mod`.

- **APK מוגמר:** `ClassicHome_v2_with_widgets.apk` (חתום debug)
- **בנייה:** `python build_and_deploy.py` · **בנייה+התקנה:** `python build_and_deploy.py --deploy`
- **תוכנית העבודה:** `UPGRADE_PLAN.md` · **מחקר עיצוב:** `plan_research/rom_settings_design/`

## הפיצ'רים שמומשו

| תחום | מה יש |
| :--- | :--- |
| שעון ותאריך | שעון + תאריך לועזי + תאריך עברי; גודל/צבע לכל אחד, ולשעון גם גופן ופורמט שעה |
| ווידג'טים | עמודי ווידג'טים דינמיים (ViewFlipper, מעבר ב-`#`), הוספה מלחיצה ארוכה בתפריט, עריכת מיקום/גודל, ווידג'ט צפייה-בלבד במסך הראשי |
| קיצורי מקשים | כל מקש (ספרות, `*`, `#`, חיצים, אישור, מקשים רכים) בלחיצה קצרה/ארוכה → אפליקציה או פעולה (פנס, וילון, שקט, יומן...) |
| תפריט היישומים | רשת בגודל מתכוונן; שיוך אפליקציות לקבוצות (תפריט/כלים/משחקים/מוסתר) |
| שורת שיחות | שורת שיחות-שלא-נענו שנעלמת כשאין |
| מסך הגדרות | עוצב מחדש 2026-08-07 בסגנון NewUI של המערכת — זהה ויזואלית לאפליקציית ההגדרות של ה-ROM |
| החלפת לאנצ'ר | מתג "מסך הבית המשודרג" עם מסלול חזרה בטוח למקורי |

## מבנה

```
מסך בית/
├── CLAUDE.md                 ← כללי הברזל + חוזה התיעוד (נטען כל סשן)
├── .claude/skills/           ← הידע התפעולי, לפי נושא:
│   ├── qin-launcher-build/   ← צנרת הבנייה, TARGET_CLASSES, משאבים
│   ├── qin-device/           ← עבודה מול המכשיר (adb, צילומים, ניווט)
│   ├── qin-newui/            ← ערכת העיצוב של ה-ROM ואיך משתמשים בה
│   ├── qin-launcher-map/     ← מפת הקוד, מפתחות ההגדרות, מנוע הקיצורים
│   └── qin-mouse-context/    ← רקע העכבר ופרויקט StarMouse
├── src/                      ← Java. רק מה שב-TARGET_CLASSES מתקמפל (10 קבצים);
│                                השאר פלט jadx לתיעוד — עריכתו לא משפיעה על ה-APK!
├── apktool_out/              ← ה-APK המפורק: smali (מקור האמת לרוב הקוד) + res + manifest
├── tools/                    ← שרשרת בנייה עצמאית (JDK חיצוני נדרש; השאר מקומי)
├── plan_research/            ← מחקרים: rom_settings_design/ (מפרט NewUI מלא + צילומי רפרנס)
├── build_and_deploy.py       ← הצנרת (5 שלבים, כולל שער drift של חתימות smali)
├── UPGRADE_PLAN.md           ← תוכנית השדרוג המלאה וסטטוס
└── README.md                 ← הוראות למשתמש הקצה
```

## פרויקט אח

`C:\Users\a0527\Desktop\עכבר במכשיר מקשים` — עכבר בלחיצה ארוכה על `*` ‏(StarMouse). ‏`GUIDE.md`
שלו מרכז 30 מלכודות מאומתות של המכשיר הזה. ידע משותף: ערכת NewUI, עבודת adb, מלכודות ROM.
